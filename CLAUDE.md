# Snake of Mercury - Phase 0 需求发现与创新决策 AI 助手

## 项目概述

基于苏格拉底式追问机制，帮助用户在信息不充分的条件下快速收敛产品方向。核心价值：边验证边澄清，而非线性等待。用户输入模糊念头，系统通过强制选择式提问在3轮内输出带置信度的需求假设清单。

## Sprint 1 实现状态 ✅

### 已完成功能

1. **创新类型判断节点** ✅
   - 第一轮强制问"颠覆式改变游戏规则"还是"渐进式改进现有方案"
   - 未问此问题，系统拒绝输出任何分析结论
   - 文件: `src/phase0-questionnaire.ts`

2. **决策卡点定位器** ✅
   - 第二轮问"你现在最大的卡点是什么？"
   - 收敛到：需求/技术/商业/规模化 四个维度
   - 根据卡点类型选择对应追问模板

3. **基础追问引擎（强制选择式）** ✅
   - 使用强制选择式提问，不允许无限开放式问答
   - 六字段状态机管理进度
   - 空白字段高亮显示但不阻断进度

### 验收标准

- [x] 用户输入任意模糊念头，系统第一轮必须问'颠覆式还是渐进式'
- [x] 未问创新类型判断问题，系统拒绝输出任何分析结论
- [x] 决策卡点定位后，能根据卡点类型选择对应追问模板

### 测试覆盖

- `phase0-questionnaire.ts`: 68.47% 覆盖率
- 全部 76 个测试通过

## 核心模块

| 文件 | 功能 |
|------|------|
| `src/phase0-questionnaire.ts` | 追问收敛模块 - Sprint 1 核心 |
| `src/state-machine.ts` | Phase 状态管理 |
| `src/convergence-detector.ts` | 收敛检测 |
| `src/rollback-manager.ts` | 回滚管理 |
| `src/types.ts` | 类型定义 |

## 技术栈

- TypeScript
- Vitest (测试)
- Zod (验证)
- @anthropic-ai/sdk (AI)
