{
  "name": "snake-of-mercury",
  "version": "1.0.0",
  "description": "Unified Autopilot - Multi-Agent Autonomous Coding Harness",
  "type": "module",
  "license": "MIT",
  "scripts": {
    "harness": "tsx src/harness-scheduler.ts",
    "harness:codex": "HARNESS_ENGINE=codex tsx src/harness-scheduler.ts",
    "harness:debug": "tsx --inspect src/harness-scheduler.ts",
    "typecheck": "tsc --noEmit",
    "test": "vitest",
    "test:run": "vitest run",
    "test:coverage": "vitest run --coverage",
    "pipeline": "HARNESS_MODE=pipeline tsx src/harness-scheduler.ts",
    "pipeline:minimax": "HARNESS_MODE=pipeline HARNESS_ENGINE=minimax tsx src/harness-scheduler.ts",
    "v3": "tsx src/main.ts",
    "build": "tsc -p tsconfig.json --noEmit",
    "discuss": "tsx src/discuss-server.ts"
  },
  "dependencies": {
    "@anthropic-ai/sdk": "^0.50.0",
    "zod": "^3.22.0"
  },
  "devDependencies": {
    "@types/node": "^20.10.0",
    "@vitest/coverage-v8": "^1.0.0",
    "tsx": "^4.7.0",
    "typescript": "^5.3.0",
    "vitest": "^1.0.0"
  },
  "engines": {
    "node": ">=22.0.0"
  }
}
